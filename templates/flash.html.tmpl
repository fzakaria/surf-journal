{{define "flash"}}
<section>
    <div class="tw:bg-red-100 tw:border tw:border-red-400 tw:text-red-700 tw:px-4 tw:py-3 tw:rounded tw:relative" role="alert">
        <span class="tw:block sm:tw:inline">{{.}}</span>
    </div>
</section>
{{end}}